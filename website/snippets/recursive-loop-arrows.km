import go { Println } from "fmt"

function main(): void {
  for (const factorial = (n: int): int => {
    if (n <= 1) { return 1 }
    return n * factorial(n - 1)
  }; factorial(0) == 1;) {
    Println(factorial(5))
    break
  }
}
